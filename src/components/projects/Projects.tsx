import ProjectCards from "./ProjectCards";

const Projects: React.FC = () => {
  return (
    <div className="w-full bg-MainPrimary border-r border-NavBorderColor">
      <div>
        <ProjectCards />
      </div>
    </div>
  );
};

export default Projects;
